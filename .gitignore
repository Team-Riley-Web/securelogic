# Dependencies
node_modules/

# Build output
dist/
.astro/

# Local Netlify folder
.netlify/

# Netlify deployment artifacts
deploy-*.zip

# OS files
.DS_Store
Thumbs.db

# IDE files
.idea/

# Environment variables
.env
.env.local
.env.production

# Dolt database files
.dolt/
*.db
.gstack/

# Client reference files (copy decks, logos, source images, OneDrive dump).
# Moved in from ~/Sites/securelogic-files on 2026-09-16 for convenience; ~180MB
# of assets that do not belong in the repo, which is PUBLIC on GitHub.
reference-files/

# Internal sales pricing guide. Marked CONFIDENTIAL / not for customer
# distribution and the repo is public, so it is kept out of git. It is the
# source of truth for how Genesis360 is packaged; see TASKS.md 2026-09-16.
src/assets/brochures/*.docx

# Subagent-driven-development scratch (ledgers, briefs, review packages)
.superpowers/

# Parallel batch worktrees
.worktrees/
